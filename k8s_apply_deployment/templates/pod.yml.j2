apiVersion: v1
kind: Pod
metadata:
  name: {{name_app}}
  namespace: {{namespace}}
spec:
  containers:
    - name: {{name_app}}
      image: {{nexus_repository_url_worker}}/{{nexus_repo}}/{{name_app}}:{{docker_image_tag}}
  imagePullSecrets:
    - name: {{secret_name}}
